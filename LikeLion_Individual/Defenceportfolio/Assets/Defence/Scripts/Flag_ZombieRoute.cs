using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Flag_ZombieRoute : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Transition is called once per frame
    void Update()
    {
        
    }
}
